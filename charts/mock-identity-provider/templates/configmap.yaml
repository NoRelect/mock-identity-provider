apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mock-identity-provider.fullname" . }}-config
  labels:
    {{- include "mock-identity-provider.labels" . | nindent 4 }}
data:
  config.json: {{ .Values.config | toJson }}
